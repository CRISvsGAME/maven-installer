[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "maven-installer"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
install-maven = "maven_installer.cli:main"
